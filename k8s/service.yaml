apiVersion: v1
kind: Service
metadata:
  name: statmaster-web
  labels:
    app: statmaster-web
spec:
  type: LoadBalancer
  selector:
    app: statmaster-web
  ports:
    - name: http
      port: 80
      targetPort: http